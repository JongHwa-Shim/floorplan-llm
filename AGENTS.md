# 유의사항 및 참고사항
- Notion MCP 사용 시에는 모든 페이지의 내용을 처음부터 다시 작성하는 것보다 최대한 토큰 소모량을 절약하기 부분 부분 삭제&추가 방식으로 수정하도록해.
- 생각 과정과 응답을 한국어로 하세요(코드나 고유단어 등 영어로 쓰는게 더 좋은 경우나, 영어로 해야 더 좋은 품질의 응답을 할 수 있는 경우는 제외).
- 실행 환경은 uv를 사용중입니다.
- wsl 환경에서 코드 작성중입니다.
- 항상 수행한 업무에 대해 검증(결과물 뽑기 등)을 수행하고, 검증 결과를 요약하여 보고하거나 저장하여 살펴볼수있도록 안내하세요. 결과를 저장해야한다면 보통 temp/에 저장하고, 업무에 따라 다른 저장 디렉토리를 안내한다면 거기에 저장하세요.
-  혼자 작업을 끝마치고 끝내지마세요. 항상 이해하기 무엇을 수행했고 뭐가 바꼈고 어떤 효과가 생기는지 알기 쉽게 보고하세요.

# 프로젝트 디렉토리 구조 가이드
- 본 프로젝트는 아래와 같은 형식의 디렉토리 구조를 사용할 것이다.
- 디렉토리 구조 및 파일들은 전부 예시이며 실제로 프로젝트를 진행시에는 다른 파일들이 작성된다.
- 이런 디렉토리 구조 느낌으로 코드를 작성&유지관리 해야한다는 것.
## 디렉토리 구조 (예시)
```text
generative_ai_project/
├── config/
│   ├── model_config.yaml
│   └── logging_config.yaml
├── data/
│   ├── cache/
│   ├── embeddings/
│   └── vectordb/
├── src/
│   ├── core/
│   │   ├── base_llm.py
│   │   ├── gpt_client.py
│   │   ├── claude_client.py
│   │   ├── local_llm.py
│   │   └── model_factory.py
│   ├── prompts/
│   │   ├── templates.py
│   │   └── chain.py
│   ├── rag/
│   │   ├── embedder.py
│   │   ├── generate.py
│   │   ├── vector_store.py
│   │   └── indexer.py
│   ├── processing/
│   │   ├── chunking.py
│   │   ├── cleaner.py
│   │   └── preprocessor.py
│   └── inference/
│       └── inference.py
├── docs/
│   ├── README.md
│   └── SETUP.md
├── scripts/
│   ├── setup_env.sh
│   ├── run_tests.sh
│   ├── build_embeddings.py
│   └── cleanup.py
├── .gitignore
├── Dockerfile
├── docker-compose.yml
└── requirements.txt
```
## 📂 디렉토리 역할 (Directory Rules) (예시)
- `config/`: 모델 및 로깅 설정 파일 (`.yaml`, `.json` 등) 전용.
- `data/`: 캐시, 임베딩, 벡터 DB 등 로컬 데이터 저장소 (Git 추적 제외).
- `src/core/`: LLM 클라이언트 추상화(GPT, Codex, 로컬 모델) 및 팩토리 로직.
- `src/prompts/`: 프롬프트 템플릿 및 체이닝 로직. (핵심 비즈니스 로직에 프롬프트 하드코딩 금지)
- `src/rag/`: 검색 컴포넌트 (임베더, 문서 생성기, 벡터 스토어, 인덱서).
- `src/processing/`: 텍스트 청킹, 정제, 전처리 파이프라인.
- `src/inference/`: 메인 추론 엔진 및 파이프라인 진입점.
- `scripts/`: 환경 설정, 테스트, 일괄 처리용 쉘/파이썬 스크립트.

## 프로젝트 디렉토리 구조 관점 코딩 규칙
1. **철저한 관심사 분리**: 프롬프트 로직, LLM API 호출, 데이터 전처리 코드를 하나의 파일에 섞지 마시오.
2. **구조 준수**: 파일 생성 및 수정 시 항상 위의 디렉토리 구조에 맞는 적절한 위치를 선택하시오.
3. **모듈화**: 함수와 클래스는 단일 책임을 가지도록 모듈화하고, 순수 함수(Pure function) 형태를 지향하시오.
4. **구조 변경 제한**: 새로운 기능이 기존 구조에 명확히 들어맞지 않는 경우, 임의로 새 루트 폴더를 생성하지 말고 개발자에게 먼저 구조 확장을 제안하시오.

# 코딩 스타일 및 주석 작성 규칙
당신의 목표는 PyTorch 기반의 딥러닝 모델(특히 LLM, VLM, 계층형 AI 에이전트 등 복잡한 아키텍처)을 위한 확장 가능하고, 재현 가능하며, 하드웨어 효율이 극대화된 코드를 작성하고 리뷰하는 것입니다.
불필요하게 장황한 설명은 생략하고, 즉시 실행 가능하며 모듈화된 완성도 높은 코드를 제공해야 합니다.
모든 Python 및 PyTorch 코드 작성 시 다음 규칙을 엄격히 준수하세요: 

## 1. **모든 주석은 한글로 달아주세요 (고유단어나 영어로 써야하는 표현들은 제외)**

## 2. **Google 스타일 Docstring 적용**
- 모든 함수, 클래스, 메서드에는 Google Style의 Docstring을 작성한다.
- 'Args', 'Returns', 'Raises' 섹션을 반드시 포함한다.
- 주석 내 수식이나 수학 기호는 반드시 LaTeX를 사용한다 (예: $E=mc^2$).
- PyTorch 모듈의 경우 'Shape' 섹션을 추가하여 입출력 텐서의 차원을 LaTeX로 명시한다 (예: $(N, C_{in}, L)$).

## 3. **텐서 형태 추적 (Shape Tracking)**
- `view`, `permute`, `flatten`, `transpose` 등 차원이 변하는 연산이나 주요 연산 직후에는 반드시 인라인 주석으로 현재 텐서의 Shape을 기록한다.
- 예시: `x = x.transpose(1, 2)  # (B, L, D)`

## 4. **블록 및 인라인 주석**
- **블록 주석**: 복잡한 로직이나 논문의 수식을 코드로 옮길 때, 코드 윗줄에 그 이유와 근거를 설명한다.
- **인라인 주석**: 코드와 최소 2칸 띄우고 작성하며, 코드 내용을 반복하기보다 작성 의도나 보정치에 대한 설명을 적는다.

## 5. **코드 품질 및 관리**
- 변수명은 약어보다 직관적인 이름을 선호한다 (예: `learning_rate`).
- 추가 구현이 필요하거나 최적화가 필요한 부분은 `# TODO: 설명` 형식을 사용한다.

## 6. 실행 효율 및 최적화
- **벡터화:** 대규모 텐서 연산은 파이썬 리스트 대신 반드시 NumPy/PyTorch 활용.
- **데이터 I/O:** GPU 병목 방지를 위해 전처리/순차 읽기 최적화. CPU-GPU 동기화 및 GPU Idle 시간 최소화.
- **연산/메모리:** 혼합 정밀도(AMP)와 최적화 커널을 적용해 Tensor Core 활용도와 배치 사이즈 극대화.

## 7. 실험 설정 및 재현성
- **설정 모듈화:** `argparse`나 하드코딩 대신 Hydra/OmegaConf(YAML) 기반 설정 및 오버라이드 사용.
- **재현성 보장:** PyTorch, NumPy, Python, CuDNN의 모든 시드(Seed)를 결정론적(Deterministic)으로 고정.
- **실험 추적:** 터미널 로그 대신 W&B 또는 MLflow를 연동해 설정값, Loss, 하드웨어 지표 자동 기록.

## 8. 코드 수정 과정 기록
- 코드 수정시 왜 코드를 수정하게 됐는지에 대한 맥락이 필요한 경우 그 이유나 시행착오를 주석으로 표기합니다(예: OpenCV의 approxPolyDP는 일반 근사를 수행하므로 직사각형 등에 대한 폴리곤에 대해 직각 보장이 안 된고 미묘한 대각선이나, 일부 요철을 생략할 수 있었다. 따라서 대신 전용 직교 폴리곤 추출 알고리즘을 사용하는 것을 수정함.).
- 코드 수정에 대한 주석은 "Mod Record:" 등의 표지자를 사용하여 표시합니다.

## 9. 문서 작성 관련
- README.md는 코드베이스의 구조, 기능, 스크립트 사용법 등 한눈에 해당 프로젝트를 이해하고 사용하기 위한 내용을,
Docs.md에는 세세한 프로젝트 설계도와 같은 내용을 작성하도록 합니다.
- 문서에 내용 추가시 내용을 어떻게 작성할지 헷갈린다면 기존에 작성되어있는 내용과 형식, 바이브를 참조하여 비슷하게 작성하세요.
